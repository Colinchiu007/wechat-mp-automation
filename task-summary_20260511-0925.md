# 微信公众号自动化 Phase 1 MVP - RewriteProcessor 修复完成

**时间**: 2026-05-11 09:25 (GMT+8)

## 目标
修复 RewriteProcessor 的 tokens_used 填充问题，使其能正确追踪 LLM API 调用的 token 使用量。

## 问题诊断

### 现象
- RewriteProcessor 调用成功，但 metadata 中 `tokens_used` 和 `word_count` 为 None
- 使用 DeepSeek API 时报错：`'function' object has no attribute 'title'`

### 根因分析
1. **Provider 不支持**: `_call_llm` 只处理 `openai` 和 `anthropic`，不支持 `deepseek`/`qwen`
2. **Usage 丢弃**: `_call_llm` 返回纯文本字符串，丢弃了 API 响应中的 `usage` 字段
3. **Config 类型**: Pydantic Config 对象传给 RewriteProcessor 时，`config.get('llm')` 返回模型对象而非 dict

## 解决方案

### 1. 添加 deepseek/qwen provider 支持
```python
if provider == "openai" or provider == "deepseek" or provider == "qwen":
    # OpenAI-compatible API format
```

### 2. 返回完整响应结构
```python
# 之前：只返回文本
return result["choices"][0]["message"]["content"]

# 现在：返回 dict 包含 content 和 usage
return {
    "content": result["choices"][0]["message"]["content"],
    "usage": result.get("usage", {})
}
```

### 3. 更新调用链
- `rewrite()`: 提取 `llm_response["content"]` 和 `usage`
- `_parse_response()`: 接收 `usage` 参数
- `metadata`: 添加 `tokens_used`, `prompt_tokens`, `completion_tokens`, `word_count`

### 4. Config 类型转换
```python
# Config.get() 返回 dict
if hasattr(value, 'model_dump'):
    return value.model_dump()
```

## 验证结果

**测试用例**: Chrome AI内存占用文章改写

```python
test = Content(
    id='test-001',
    title='Chrome AI内存占用过高',
    content='Chrome正在将大量AI功能塞进浏览器...',
)
result = await processor.rewrite(test, cfg)
```

**输出**:
```
success: True
title: Chrome浏览器因集成AI功能导致内存消耗激增
chars: 721
metadata: {
    'strategy': 'paraphrase',
    'original_length': 64,
    'rewritten_length': 721,
    'word_count': 721,
    'tokens_used': 540,
    'prompt_tokens': 142,
    'completion_tokens': 398
}
```

✅ **测试通过**

## 修改文件
- `src/processors/rewrite.py`: 
  - `_call_llm()`: 返回 `{content, usage}` dict
  - `rewrite()`: 提取 usage 并传递
  - `_parse_response()`: 接收 usage 参数
  - metadata 添加 tokens 相关字段
  - provider 添加 deepseek/qwen 支持
- `src/config/loader.py`: 
  - `Config.get()`: Pydantic 模型转 dict

## Git 提交
```
commit 41ebb6b
fix(rewrite): track LLM tokens_used and support deepseek/qwen providers
```

## 下一步
1. 测试 RSS 采集模块
2. 测试 WorkflowEngine 完整流程
3. 测试公众号发布器（wechat_mp.py）
4. 端到端集成测试：RSS → 改写 → 排版 → 草稿箱

## 技术债务记录
- `_build_prompt` 中的 `{content.title}` 在某些边界情况下可能出错（之前的 function object 错误未完全定位）
- 需要添加更多 provider 的单元测试覆盖
